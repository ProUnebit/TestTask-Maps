// import { testFunction } from './styling'

// let arr = [2, 4, 5]

// let newArr = arr.map((a, i) => `<div>${a*i}</div>`).join('')

// let nodeItem = document.querySelector('.wrapper')

// testFunction(nodeItem, newArr);